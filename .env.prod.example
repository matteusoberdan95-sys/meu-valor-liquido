# Copie para .env.prod e preencha antes de: docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build

POSTGRES_DB=meu_valor_liquido
POSTGRES_USER=meu_valor_liquido
POSTGRES_PASSWORD=CHANGE_ME_STRONG

WEBAPP_PORT=8080
SITE_BASE_URL=https://meuvalorliquido.com.br

ADS_ENABLED=false

MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USE_SSL=true
MAIL_USER=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS=noreply@meuvalorliquido.com.br
MAIL_FROM_NAME=Meu Valor Líquido
