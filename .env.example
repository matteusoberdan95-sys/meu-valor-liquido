# Meu Valor Líquido — variáveis Docker Compose (copie para .env)

POSTGRES_DB=meu_valor_liquido
POSTGRES_USER=meu_valor_liquido
POSTGRES_PASSWORD=change-me-local

# Portas expostas no host
WEBAPP_PORT=8080
POSTGRES_PORT=5432
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025

# URL pública usada em canonical/OG/sitemap
SITE_BASE_URL=http://localhost:8080
